import 'package:flutter/cupertino.dart';

class Score {
  // coordinate;
  // player;
  // final double width;
  // final double height;
  // double sixMeterRadius;
  // double nineMeterRadius;

  // Score({this.width, this.height}) {
  //   initFixPoints();
  // }

  // initFixPoints() {
  //   sixMeterRadius = width / 2;
  //   nineMeterRadius = width / 3;
  // }

  calculatePosition(Offset position) {
    double x = position.dx;
    double y = position.dy;
    print("x");
    print(x);
    // print(width);
    // print("y");
    // print(y);
    // print(height);

    // double screenWidth = MediaQuery.of(context).size.width;
    // double screenHeight = MediaQuery.of(context).size.height;
  }

  // checkInnerCircle()) {

  //       details.globalPosition

  // }
}
